<h1>Pitches#show</h1>
<p>Find me in app/views/pitches/show.html.erb</p>

<h2><%= @pitch.title %></h2>
<p><%= @pitch.summary %></p>
<p><%= @pitch.description %></p>

<%= link_to 'Edit', edit_pitch_path(@pitch.id) %>
<%= link_to 'Delete', pitch_path(@pitch.id), method: :delete %>
<%= link_to 'Back', pitch_path %>
